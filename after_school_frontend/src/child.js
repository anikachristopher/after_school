class Child{
    constructor(name, age, grade){
        this.name = name;
        this.age = age;
        this.grade = grade
        //whatever behaviour belongs to this class will be stored here. Will have a render child method.
        //should my constructor include an id attribute
        //cretae a render child method to show the child and associate with an ID
    }

    // render newChild()


    //     fetch(`${BASE_URL}/children`)
    //     .then(resp => resp.json())
    //     .then(children => {
    //         for (const child of children){
    //             console.log(child)
    //             let c = new Child(child.name, child.age, child.grade)
    //     })
    // }
}

